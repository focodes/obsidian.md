`<!DOCTYPE html>` ->Es una declaración que define el tipo de archivo
`<html lang="ES">` -> Permite declarar el nombre y el país 

``` document.gerElementById(x).innerHTML ```es una declaración
`<base>`
`<video>`  `<source>`
`<picture>` `<source>` especifican imágenes para determinadas dimensiones de la pantalla

<a hrfe= "https://www.w3schools.com/">W3school</a>
CSS flexbox
BEM (Block, element, Modifier)
display: flex
class = "block-name"
Nombre de los elementos class="block-name__element-name"
Los estilos se pueden reutilizar solo anida un elemento dentro de de un bloque

##### Modelo de caja
##### Modelo flexbox
Definir un contenedor flexible - Elemento padre. Estebede tener un elemento principal con la propiedad de visualización establecida en flex

#### Propiedades del contenedor flexible
flex-direction ->Define en que dirección apilar los elementos
`flex-wrap`-> Especifica si los elementos deben envolverse o no
`flex-flow`-> Propiedad abreviada de las dos anteriores
`justify-content`-> Alinear elementos flexibles
`aling-items`->Alinear articulos flexibles
`aling-content` -> Alinear lineas flexibles
Margen -> Crear espacio alrededor 

>Es el fin del diseño web en un lugar donde tu te sientas libre de diseñar y se te genere el código

Objeto de investigación ->¿se puede dormir menos de lo habitual? ¿cómo ser más productvo y tener más horas para otras cosas?

<a href="https://jekyllrb.com/">Jekyll</a>
Principios del conocimiento interconectado(nada existe de manera aislada), infinito
Pluggin -> Programas complentarios que amplian las fucnionalidades de un sistema

### Aprendizaje basado en proyectos
Se presenta el problema -> Se identifican las necesidades de aprendizaje -> se localiza la información necesaria -> se resuelve el problema-> se identifican nuevos
 